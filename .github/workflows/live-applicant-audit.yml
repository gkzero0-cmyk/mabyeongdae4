name: Live Applicant Detail Audit

on:
  push:
    branches:
      - feature/admin-history-20260911

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm test
      - run: node scripts/audit-live-applicant-details.js
